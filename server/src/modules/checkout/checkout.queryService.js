/**
 * CheckoutQueryService — READ operations for the checkout module.
 *
 * Currently the checkout module is write-only (session creation, webhook
 * handling, fulfillment).  This service exists as a placeholder so the
 * Query / Write split is in place when read operations are added later
 * (e.g. "get checkout session status", "list recent checkouts for admin").
 */
export class CheckoutQueryService {
  // No read operations exist yet — add them here as the module grows.
}
